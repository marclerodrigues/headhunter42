<div class="box">
  <div class="box-header">
    <h3 class="box-title"><strong><%= t("step") %></strong>: <%= @step.name %></h3>

    <div class="box-tools">
      <div class="input-group input-group-sm" style="width: 200px;">
        <input name="table_search" class="form-control pull-right" placeholder="Search" type="text">

        <div class="input-group-btn">
          <button type="submit" class="btn btn-default">
            <i class="fa fa-search"></i>
          </button>
        </div>
      </div>
    </div>
  </div>

  <div class="box-body table-responsive no-padding">
    <table class="table table-hover">
      <tbody>
        <tr>
          <th><%= t('name') %></th>
          <th><%= t('description') %></th>
        </tr>
        <% @skills.each do |skill| %>
        <tr>
          <td><%= skill.name %></td>
          <td><%= skill.description %></td>
          <td style="width: 150px">
            <%= link_to admin_skill_path(skill), method: :get, class: "btn btn-primary btn-circle" do %>
                <i class="fa fa-eye"></i>
            <% end %>
            <%= link_to edit_admin_skill_path(skill), method: :get, class: "btn btn-success btn-circle" do %>
              <i class="fa fa-edit"></i>
            <% end %>
            <%= link_to admin_skill_path(skill), method: :delete, class: "btn btn-danger btn-circle", data: {confirm: t("messages.confirm_with")} do %>
                <i class="fa fa-remove"></i>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
